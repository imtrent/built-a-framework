## 👨‍🎨 Build a Framework (W.I.P)

Build a mini CSS framework with the styles your website or app only needs.

### 👨🏻‍💻 Development

```bash
$ git clone https://github.com/imtrent/build-a-framework.git
$ yarn install
$ yarn start
```

This will serve the files at `http://localhost:3000/`